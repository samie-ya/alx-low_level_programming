#include "main.h"
/**
* _isdigit- return 1 to digit
*
* @c: paarameter used
*
* Return: int
*/
int _isdigit(int c)
{
if (c == '0' && c <= '9')
{
return (1);
}
else
{
return (0);
}
_putchar('\n');
return(0);
}
