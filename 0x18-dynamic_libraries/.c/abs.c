#include "main.h"
/**
* _abs- to print out absolute value
*
* @r: is the parameter used
*
* Return: value 0
*/
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
_putchar('\n');
return (0);
}
