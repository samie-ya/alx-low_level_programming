#include <stdio.h>
/**
 * main - will be printing a statement and returning zero value.
 * Return: Always 0.
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
