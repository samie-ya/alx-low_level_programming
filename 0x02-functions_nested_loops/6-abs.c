#include "main.h"
/**
* _abs- to print out absolute value
*
* @r: is the parameter used
*
* Return: value 0
*/
int _abs(int r)
{
if (r >= 0)
{
return (r);
}
else
{
return (-r);
}
_putchar('\n');
return (0);
}
