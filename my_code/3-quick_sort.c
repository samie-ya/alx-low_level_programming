#include "sort.h"

/**
*
*
*
*
*
*
*
*
*
*
*/

void quick_sort(int *array, size_t size)
{
	int pivot;
	size_t i, *p1 = 0, *p2 = 0;

	pivot = array[size - 1];
	for (i = 0; i < size; i++)
	{
		if (*p1 < pivot)
		{
			*p1 == i + 1;
			*p2 == i + 1;
		}
		else
		{
			
		}
	}
}
