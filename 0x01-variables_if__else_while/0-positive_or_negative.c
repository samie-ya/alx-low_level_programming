#include <stdio.h>
/**
 * main- will be executing if else function and return zero.
 * Return: 0
 */
int main(void)
{
int n;
scanf("%d\n", &n);
if
(n < 0);
{
printf("%d is negative\n", n);
}
else if
(n > 0);
{
printf("%d is positive\n", n);
}
else
{
printf("%d is zero\n", n);
}
return (0);
}
